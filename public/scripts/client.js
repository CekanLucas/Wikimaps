$(document).ready(() => {
/*   $('.btn.btn-warning')

  $('login-button').click( e => {

    $.ajax({
      method: 'post',
      url: 'http://localhost:8080/login',
      data:
    })
    .then(res => {})
  }) */





});
